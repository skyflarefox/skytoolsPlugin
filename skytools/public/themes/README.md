Coloque temas personalizados nesta pasta.

Cada tema deve ser um arquivo `.css` com nome simples, por exemplo `meu-tema.css`.
O SkyTools copia estes arquivos para o cache `webkit` quando precisa carregar a interface,
mas esta pasta `public/themes` e a fonte que deve ser compartilhada ou preservada.
